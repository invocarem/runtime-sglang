{
  "tools": [
    {
      "id": "echo",
      "label": "Echo",
      "description": "Return the input text back to the caller.",
      "schema": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string",
            "description": "Text to echo back."
          }
        },
        "required": ["text"],
        "additionalProperties": false
      }
    },
    {
      "id": "health_check",
      "label": "Health Check",
      "description": "Return a simple runtime health status.",
      "schema": {
        "type": "object",
        "properties": {
          "verbose": {
            "type": "boolean",
            "description": "Include extra details if true."
          }
        },
        "additionalProperties": false
      }
    }
  ]
}
